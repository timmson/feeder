feeder:
  version: "8"
  tg:
    token: "x"
    owner:
      id: "1000"
      name: "name"
  stock:
    timeout: 10000
    schedule: "0 5 10,17 * * *"

spring:
  output:
    ansi:
      enabled: always

server:
  port: 9090